The cedar-detect-server binary is a gRPC server that implements the CedarDetect
service declared at ../proto/cedar_detect.proto.

You can use the binary provided here, or you can build it from the Rust source
at https://github.com/smroid/cedar-detect.
